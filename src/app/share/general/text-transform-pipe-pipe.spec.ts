import { TextTransformPipePipe } from './text-transform-pipe-pipe';

describe('TextTransformPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TextTransformPipePipe();
    expect(pipe).toBeTruthy();
  });
});
